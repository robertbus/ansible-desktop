---
- name: Install Ubuntu provided packages
  hosts: localhost

  tasks:
    - name: Install packages
      become: yes
      become_user: root
      become_method: sudo
      apt: name={{item}} state=present
      with_items:
        # sytem debugging
        - htop
        # programming
        - meld
